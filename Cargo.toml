[workspace]
members = [
    "frontend",
    "gameboy"
]

[profile.dev]
opt-level = 3
overflow-checks = false

[profile.release]
lto = true
panic = "abort"
codegen-units = 1
debug = true # flamegraph only